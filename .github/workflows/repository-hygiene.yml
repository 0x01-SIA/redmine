name: Repository Hygiene

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run repository hygiene check
        run: |
          chmod +x script/check_repository_hygiene
          script/check_repository_hygiene
